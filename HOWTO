#compiling the demo program:
g++ -o demo1 demo1.cc -lglut -lGLU -lGL

#os x
g++ -o demo1 demo1.cc -framework GLUT -framework OpenGL

# running the program

./demo1 scenes/MIT_teapot.obj 


#for a makefile (all of the below):
#default cmdline flags
LDFLAGS	= -lGL -lglut -lGLU
# Mac OS alternate cmdline link options
ifeq "$(OSTYPE)" "Darwin"
	LDFLAGS	= -framework Carbon -framework OpenGL -framework GLUT
endif